name: Dragon DiskForge Beta Release Publish Contract

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: dragon-beta-release-publish-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    name: Fail-closed beta release bundle/publish contract
    runs-on: windows-latest

    steps:
      - name: Checkout exact source
        uses: actions/checkout@v4

      - name: PowerShell 7 release-publish self-test
        shell: pwsh
        run: ./scripts/beta-release-publish.ps1 -Mode self-test

      - name: Windows PowerShell 5.1 release-publish self-test
        shell: powershell
        run: .\scripts\beta-release-publish.ps1 -Mode self-test
